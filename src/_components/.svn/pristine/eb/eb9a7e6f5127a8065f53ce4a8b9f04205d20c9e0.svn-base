import React from 'react'
import { Dropdown } from './Dropdown'

export default function(props) {
  // Declare a new state variable, which we'll call "count"
  // const [count, setCount] = useState(0)

  return <Dropdown {...props} inputSelect />
}
