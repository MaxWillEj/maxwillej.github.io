import styled from 'styled-components'

export const LabelEllipsis = styled.pre`
  margin: 0;
  padding: 0;
  font-size: 14px;
  font-family: Nunito Sans;
  font-weight: 600;
  letter-spacing: 0.04em;
  color: #3f4658;
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
  flex: ${props => (props.flex ? props.flex : '')};
`
